const kiwis = 5 * 5
const peras = 3 * 2
const uvas = 4 / 2
const total = document.querySelector('p')

const suma = kiwis + peras + uvas

console.log (suma)

total.innerHTML = suma + total.innerHTML 

